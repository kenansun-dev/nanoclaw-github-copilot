# Terraform
.terraform/
*.tfstate
*.tfstate.*
tfplan
crash.log
crash.*.log

# Local secrets — only the .example is committed
terraform.tfvars
*.auto.tfvars
override.tf
override.tf.json
*_override.tf
*_override.tf.json
